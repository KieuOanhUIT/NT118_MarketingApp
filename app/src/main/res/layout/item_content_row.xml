<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="6dp"
    app:cardCornerRadius="12dp"
    app:cardElevation="3dp"
    android:foreground="?attr/selectableItemBackground"
    android:layout_marginHorizontal="8dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:padding="12dp">

        <!-- Header Row: Tiêu đề + nút hành động (View / Edit / Delete) -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <TextView
                android:id="@+id/tvTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Tiêu đề bài viết"
                android:textSize="16sp"
                android:textStyle="bold"
                android:textColor="@color/textPrimary"/>

            <!-- Action Buttons -->
            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal">

                <!-- View button (thêm) -->
                <ImageButton
                    android:id="@+id/btnView"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_content"
                    android:contentDescription="Xem content"
                    app:tint="@color/colorSecondary"
                    android:layout_marginEnd="4dp"/>

                <!-- Edit button -->
                <ImageButton
                    android:id="@+id/btnEdit"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_edit"
                    android:contentDescription="Sửa content"
                    app:tint="@color/colorPrimary"
                    android:layout_marginEnd="4dp"/>

                <!-- Delete button -->
                <ImageButton
                    android:id="@+id/btnDelete"
                    android:layout_width="36dp"
                    android:layout_height="36dp"
                    android:background="?attr/selectableItemBackgroundBorderless"
                    android:src="@drawable/ic_delete"
                    android:contentDescription="Xóa content"
                    app:tint="@color/deadlineOverdue"/>
            </LinearLayout>
        </LinearLayout>

        <!-- Thông tin chi tiết -->
        <TextView
            android:id="@+id/tvType"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Loại: Article"
            android:textSize="14sp"
            android:layout_marginTop="4dp"
            android:textColor="#555"/>

        <TextView
            android:id="@+id/tvChannel"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Kênh: Blog"
            android:textSize="14sp"
            android:textColor="#555"/>

        <TextView
            android:id="@+id/tvTag"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Thẻ: SEO"
            android:textSize="14sp"
            android:textColor="#555"/>

        <TextView
            android:id="@+id/tvCreatedTime"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Tạo lúc: 2025-10-20T09:00:00"
            android:textSize="13sp"
            android:textColor="#777"/>

        <TextView
            android:id="@+id/tvUrl"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="URL: https://example.com/post-1"
            android:textSize="13sp"
            android:autoLink="web"
            android:layout_marginTop="4dp"
            android:textColor="@color/colorPrimary"/>

        <!-- Nút trạng thái -->
        <Button
            android:id="@+id/btnStatus"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Trạng thái"
            android:backgroundTint="@color/colorPrimary"
            android:textColor="@color/background"
            android:textAllCaps="false"
            android:paddingHorizontal="12dp"
            android:paddingVertical="6dp"
            android:layout_marginTop="6dp"/>
    </LinearLayout>
</androidx.cardview.widget.CardView>
