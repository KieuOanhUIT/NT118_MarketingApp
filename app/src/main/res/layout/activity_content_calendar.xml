<?xml version="1.0" encoding="utf-8"?>
<!-- Content Calendar Layout - Màn hình lịch đăng content -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingTop="32dp"
    android:orientation="vertical"
    android:background="#FFFFFF">

    <!-- Header Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:background="@color/backgroundCard"
        android:elevation="4dp"
        android:paddingTop="16dp"
        android:paddingBottom="16dp">

        <!-- Title and Add Button Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:background="#FF3CC7"
            android:gravity="center_vertical"
            android:paddingRight="16dp"
            android:paddingLeft="16dp"
            android:layout_marginBottom="16dp">

            <!-- Back Button -->
            <ImageButton
                android:id="@+id/btnBack"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_back"
                android:contentDescription="Back button"
                app:tint="#FFFFFF"/>

            <TextView
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="@string/content_calendar_title"
                android:textSize="22sp"
                android:textStyle="bold"
                android:gravity="center"
                android:textColor="#FFFFFF"
                android:padding="14dp"
                android:background="#FF3CC7"/>

            <Button
                android:id="@+id/btnAddContent"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:text="@string/btn_add_content"
                android:backgroundTint="#007C77"
                android:textColor="#FFFFFF"
                android:drawableStart="@drawable/ic_add"
                android:drawableTint="@color/backgroundCard"
                android:paddingStart="12dp"
                android:paddingEnd="12dp"/>
        </LinearLayout>

        <!-- Week Navigation Row -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:gravity="center_vertical">

            <ImageButton
                android:id="@+id/btnPreviousWeek"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_back"
                android:contentDescription="@string/previous_week"
                app:tint="#007C77"/>

            <TextView
                android:id="@+id/tvWeekRange"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="21/10/2025 - 27/10/2025"
                android:textSize="16sp"
                android:textColor="@color/textPrimary"
                android:gravity="center"
                android:textStyle="bold"/>

            <ImageButton
                android:id="@+id/btnNextWeek"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@drawable/ic_back"
                android:rotation="180"
                android:contentDescription="@string/next_week"
                app:tint="#007C77"/>
        </LinearLayout>
    </LinearLayout>

    <!-- Calendar Grid Container -->
    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:fillViewport="true"
        android:scrollbars="none">

        <ScrollView
            android:layout_width="wrap_content"
            android:layout_height="match_parent"
            android:fillViewport="true">

            <LinearLayout
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="8dp">

                <!-- Day Headers Row -->
                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="horizontal"
                    android:background="@color/backgroundCard"
                    android:elevation="2dp"
                    android:layout_marginBottom="4dp">

                    <!-- Empty corner for hour column -->
                    <TextView
                        android:layout_width="60dp"
                        android:layout_height="50dp"
                        android:text="@string/hour_label"
                        android:textSize="12sp"
                        android:textStyle="bold"
                        android:textColor="@color/textPrimary"
                        android:gravity="center"
                        android:background="@color/inputBackground"/>

                    <!-- Day headers will be added programmatically -->
                    <LinearLayout
                        android:id="@+id/dayHeadersContainer"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:orientation="horizontal"/>
                </LinearLayout>

                <!-- Calendar Grid - Hours x Days -->
                <LinearLayout
                    android:id="@+id/calendarGridContainer"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:orientation="vertical"/>
            </LinearLayout>
        </ScrollView>
    </HorizontalScrollView>

</LinearLayout>
