<?xml version="1.0" encoding="utf-8"?>
<!-- 
    Popup xác nhận xóa content
    Gồm overlay tối + popup container với message và 2 nút (Xóa, Hủy)
-->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/popupOverlay"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_popup_overlay"
    android:clickable="true"
    android:focusable="true">

    <!-- Popup Container -->
    <LinearLayout
        android:id="@+id/popupContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginStart="32dp"
        android:layout_marginEnd="32dp"
        android:background="@drawable/bg_popup_delete"
        android:orientation="vertical"
        android:padding="24dp"
        android:elevation="8dp">

        <!-- Warning Icon -->
        <ImageView
            android:id="@+id/imgWarning"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_gravity="center_horizontal"
            android:src="@drawable/ic_warning"
            android:contentDescription="Warning icon"
            android:background="@android:color/transparent"
            android:layout_marginBottom="16dp"/>

        <!-- Title -->
        <TextView
            android:id="@+id/tvPopupTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Xác nhận xóa"
            android:textSize="20sp"
            android:textStyle="bold"
            android:textColor="@color/textPrimary"
            android:gravity="center"
            android:background="@android:color/transparent"
            android:layout_marginBottom="12dp"/>

        <!-- Message -->
        <TextView
            android:id="@+id/tvPopupMessage"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Bạn có chắc chắn muốn xóa content này không?\n\nHành động này không thể hoàn tác."
            android:textSize="14sp"
            android:textColor="@color/textSecondary"
            android:gravity="center"
            android:lineSpacingExtra="4dp"
            android:background="@android:color/transparent"
            android:layout_marginBottom="24dp"/>

        <!-- Content Name (optional - can be set programmatically) -->
        <TextView
            android:id="@+id/tvContentName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:visibility="gone"
            android:text=""
            android:textSize="14sp"
            android:textStyle="bold"
            android:textColor="@color/colorPrimary"
            android:gravity="center"
            android:padding="12dp"
            android:background="@android:color/transparent"
            android:layout_marginBottom="24dp"/>

        <!-- Buttons Container -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:weightSum="2"
            android:gravity="center">

            <!-- Cancel Button -->
            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/btnCancel"
                android:layout_width="0dp"
                android:layout_height="50dp"
                android:layout_weight="1"
                android:layout_marginEnd="8dp"
                android:background="@drawable/bg_button_cancel"
                android:text="Hủy"
                android:textSize="15sp"
                android:textStyle="bold"
                android:textColor="#007C77"
                android:textAllCaps="false"/>

            <!-- Delete Button -->
            <androidx.appcompat.widget.AppCompatButton
                android:id="@+id/btnDelete"
                android:layout_width="0dp"
                android:layout_height="50dp"
                android:layout_weight="1"
                android:layout_marginStart="8dp"
                android:background="@drawable/bg_button_delete"
                android:text="Xóa"
                android:textSize="15sp"
                android:textStyle="bold"
                android:textColor="@android:color/white"
                android:textAllCaps="false"/>

        </LinearLayout>

    </LinearLayout>

</FrameLayout>
